export class Location {
  id: number;
  name: number;
  ownerId: number;


  constructor(id: number, name: number, ownerId: number) {
    this.id = id;
    this.name = name;
    this.ownerId = ownerId;
  }
}
